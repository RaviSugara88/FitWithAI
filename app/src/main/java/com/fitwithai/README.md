# FitWithAi — source layout

This document mirrors the intended package tree under `com.fitwithai`. Stub types are placeholders for upcoming features; production code lives in the same packages alongside them.

See the project brief in the repository root for Gradle and module setup (`:app`).
